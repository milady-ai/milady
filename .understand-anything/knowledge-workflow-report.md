# Understand Knowledge Workflow

Target: `/Users/home/Documents/milady`

Result: not a Karpathy-pattern wiki.

The detector reported no root `index.md`, no `log.md`, no `raw/` source layer, and format `unknown`. The repository has many Markdown files, but it is a product/code repository rather than the three-layer wiki shape expected by `understand-knowledge`.

Raw detection output is saved in `.understand-anything/tmp/knowledge-parse.stdout` and `.understand-anything/tmp/knowledge-parse.stderr`.