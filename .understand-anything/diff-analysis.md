# Understand Diff Analysis

No tracked working-tree changes were found with `git diff --name-only`.

The only current untracked repo artifact is `.understand-anything/`, which is generated output from these workflows and is not represented as product-source change in the graph.