diff --git a/apps/app-lifeops/src/lifeops/schedule-sync-contracts.ts b/apps/app-lifeops/src/lifeops/schedule-sync-contracts.ts
index b2787b374d..a063636e27 100644
--- a/apps/app-lifeops/src/lifeops/schedule-sync-contracts.ts
+++ b/apps/app-lifeops/src/lifeops/schedule-sync-contracts.ts
@@ -35,6 +35,21 @@ export const LIFEOPS_SCHEDULE_STATE_SCOPES = ["local", "cloud"] as const;
 export type LifeOpsScheduleStateScope =
   (typeof LIFEOPS_SCHEDULE_STATE_SCOPES)[number];
 
+// Cloud-compat shim: service-mixin-reminders imports LIFEOPS_SCHEDULE_OBSERVATION_STATES
+// which was renamed/removed on some branches. Re-export the circadian states array under
+// the legacy name so cloud builds don't break with missing-export errors at boot.
+export const LIFEOPS_SCHEDULE_OBSERVATION_STATES = [
+  "awake",
+  "winding_down",
+  "sleeping",
+  "waking",
+  "napping",
+  "unclear",
+] as const;
+
+export type LifeOpsScheduleObservationState =
+  (typeof LIFEOPS_SCHEDULE_OBSERVATION_STATES)[number];
+
 export interface LifeOpsScheduleObservationSnapshot {
   effectiveDayKey: string;
   localDate: string;
diff --git a/packages/typescript/src/utils/crypto-compat.ts b/packages/typescript/src/utils/crypto-compat.ts
index 04aadf9f7c..f1767e918c 100644
--- a/packages/typescript/src/utils/crypto-compat.ts
+++ b/packages/typescript/src/utils/crypto-compat.ts
@@ -17,23 +17,32 @@
  * ```
  */
 import { cbc, gcm } from "@noble/ciphers/aes.js";
-import { md5, sha1 } from "@noble/hashes/legacy";
+// @ts-ignore noble hash subpath typings are inconsistent across published versions.
+import { md5, sha1 } from "@noble/hashes/legacy.js";
+// @ts-ignore noble hash subpath typings are inconsistent across published versions.
 import { ripemd160 } from "@noble/hashes/ripemd160";
 import { sha224, sha256, sha384, sha512 } from "@noble/hashes/sha2";
 import * as BufferUtils from "./buffer";
 
-type SyncHashFactory = typeof sha256;
+type SyncHashInstance = {
+	update(data: Uint8Array): SyncHashInstance;
+	digest(): Uint8Array;
+};
+
+type SyncHashFactory = {
+	create(): SyncHashInstance;
+};
 type BufferEncodingName = "utf8" | "utf-8" | "base64" | "hex";
 type OwnedUint8Array = Uint8Array<ArrayBuffer>;
 
 const HASH_ALGORITHMS: Record<string, SyncHashFactory> = {
-	md5,
-	ripemd160,
-	sha1,
-	sha224,
-	sha256,
-	sha384,
-	sha512,
+	md5: md5 as SyncHashFactory,
+	ripemd160: ripemd160 as SyncHashFactory,
+	sha1: sha1 as SyncHashFactory,
+	sha224: sha224 as SyncHashFactory,
+	sha256: sha256 as SyncHashFactory,
+	sha384: sha384 as SyncHashFactory,
+	sha512: sha512 as SyncHashFactory,
 };
 
 const AES_BLOCK_SIZE = 16;
--- a/packages/agent/src/api/chat-routes.ts	2026-04-23 12:18:37.019060844 +0000
+++ b/packages/agent/src/api/chat-routes.ts	2026-04-23 12:18:58.133559447 +0000
@@ -1897,12 +1897,13 @@
         sendChunk({}, "stop");
         writeSseData(res, "[DONE]");
       } catch (err) {
+        console.error("[CHAT_DIAG] /v1/chat/completions stream catch:", err instanceof Error ? err.stack : err);
         if (!aborted) {
           writeSseData(
             res,
             JSON.stringify({
               error: {
-                message: getErrorMessage(err),
+                message: `${getErrorMessage(err)} | DIAG: ${err instanceof Error && err.stack ? err.stack.replace(/\n/g, " <<NL>> ") : "no-stack"}`,
                 type: "server_error",
               },
             }),
@@ -1984,9 +1985,10 @@
         ],
       });
     } catch (err) {
+      console.error("[CHAT_DIAG] /v1/chat/completions non-stream catch:", err instanceof Error ? err.stack : err);
       json(
         res,
-        { error: { message: getErrorMessage(err), type: "server_error" } },
+        { error: { message: `${getErrorMessage(err)} | DIAG: ${err instanceof Error && err.stack ? err.stack.replace(/\n/g, " <<NL>> ") : "no-stack"}`, type: "server_error" } },
         500,
       );
     }
@@ -2173,12 +2175,13 @@
         );
         writeSseJson(res, { type: "message_stop" }, "message_stop");
       } catch (err) {
+        console.error("[CHAT_DIAG] /v1/messages stream catch:", err instanceof Error ? err.stack : err);
         if (!aborted) {
           writeSseJson(
             res,
             {
               type: "error",
-              error: { type: "server_error", message: getErrorMessage(err) },
+              error: { type: "server_error", message: `${getErrorMessage(err)} | DIAG: ${err instanceof Error && err.stack ? err.stack.replace(/\n/g, " <<NL>> ") : "no-stack"}` },
             },
             "error",
           );
@@ -2255,9 +2258,10 @@
         usage: { input_tokens: 0, output_tokens: 0 },
       });
     } catch (err) {
+      console.error("[CHAT_DIAG] /v1/messages non-stream catch:", err instanceof Error ? err.stack : err);
       json(
         res,
-        { error: { type: "server_error", message: getErrorMessage(err) } },
+        { error: { type: "server_error", message: `${getErrorMessage(err)} | DIAG: ${err instanceof Error && err.stack ? err.stack.replace(/\n/g, " <<NL>> ") : "no-stack"}` } },
         500,
       );
     }
