# Milaidy Environment Variables

# ── Discord Bot Configuration ──────────────────────────────────────────────
# Required for cloud onboarding Discord OAuth flow
# Get these from https://discord.com/developers/applications
VITE_DISCORD_CLIENT_ID=your_discord_app_client_id
VITE_DISCORD_REDIRECT_URI=https://yourdomain.com/discord-callback

# ── Cloud Provider Configuration ───────────────────────────────────────────
# Elizacloud API base URL (default: https://www.elizacloud.ai)
# ELIZAOS_CLOUD_BASE_URL=https://www.elizacloud.ai

# Cloud container port for direct API calls (default: 2187)
# Used for Discord OAuth and other container-level operations
# CLOUD_CONTAINER_PORT=2187

# ── API Server Configuration ───────────────────────────────────────────────
# Port for the API server (default: 31337)
# MILADY_API_PORT=31337

# Bind address (default: 127.0.0.1, use 0.0.0.0 for remote access)
# MILADY_API_BIND=127.0.0.1

# ── Database Configuration ─────────────────────────────────────────────────
# PostgreSQL connection (if using Supabase or external DB)
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# ── Secrets (DO NOT COMMIT) ────────────────────────────────────────────────
# API keys should be set via:
# - Environment variables
# - Secure secret management
# - Runtime configuration

# Example:
# ANTHROPIC_API_KEY=sk-ant-...
# OPENAI_API_KEY=sk-...
# ELIZAOS_CLOUD_API_KEY=eliza_...
