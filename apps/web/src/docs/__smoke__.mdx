---
title: MDX Smoke Test
---

# MDX smoke test

This file exists **only** to verify the MDX + Shiki + remark-gfm toolchain wired up in `vite.config.ts` builds and imports correctly. It will be deleted in Phase 2 when the real docs shell lands.

## Features under test

- MDX compilation via `@mdx-js/rollup`
- `remark-gfm` — GitHub-flavored markdown (tables, strikethrough, task lists)
- `rehype-slug` — heading IDs
- `rehype-autolink-headings` — clickable anchors
- `@shikijs/rehype` — syntax highlighting

### Code block (TypeScript)

```ts
import { defineConfig } from "vite";

export default defineConfig({
  root: ".",
  server: { port: 2139 },
});
```

### Code block (Bash)

```bash
cd apps/web
bun run dev
```

### Table (remark-gfm)

| Tier | Pages | Owner |
| --- | --- | --- |
| Beginner | 7 | new users |
| Intermediate | 6 | curious users |
| Advanced | 5 | power users |
| Developer | 1 | link to docs.milady.ai |

### Task list (remark-gfm)

- [x] MDX parser wired
- [x] Syntax highlighter wired
- [ ] Real docs shell (Phase 2)
