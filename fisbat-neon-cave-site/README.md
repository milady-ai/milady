# Fisbat Neon Cave Site

Static Cloudflare Pages test site for:

`botdick.fisbat-neon-cave-site.milaidy.agency`

The site is plain HTML, CSS, and JavaScript. It has no build step and can be
deployed by uploading this folder directly.
