# /electrobun-menu-tray

Expose stable actions through desktop surfaces.

Include:

- Application menu roles and custom actions.
- Context menu for selection/view-specific actions.
- Tray menu for background/status actions.
- Command palette entries mirroring the same typed action identifiers.
- Deep-link route if platform support exists.
- Shared action dispatcher and permission/confirmation gates.
- Tests or manual smoke steps for each surface.
